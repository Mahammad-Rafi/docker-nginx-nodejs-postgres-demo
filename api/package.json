{
  "name": "docker-nginx-nodejs-postgres-demo-api",
  "version": "1.0.0",
  "description": "Express API for the Docker Nginx PostgreSQL demo",
  "main": "server.js",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check db.js && node --check routes/users.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "pg": "^8.16.3"
  }
}

